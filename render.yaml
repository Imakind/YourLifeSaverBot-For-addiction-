services:
  - type: worker
    name: your-life-saver-bot
    runtime: docker
    autoDeploy: true
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: BOT_DB_PATH
        value: /data/bot.sqlite3
    disk:
      name: bot-data
      mountPath: /data
      sizeGB: 1
